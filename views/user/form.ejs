<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artist Pitch - 777heaven Records</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #e4e4e7;
            min-height: 100vh;
            padding: 2rem 1rem;
            animation: fadeIn 0.3s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
        }

        header {
            text-align: center;
            margin-bottom: 3rem;
            position: relative;
        }

        .logo {
            font-size: 2.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 0.5rem;
        }

        .tagline {
            font-size: 1.1rem;
            color: #a1a1aa;
            margin-bottom: 2rem;
        }

        /* Hidden Admin Button */
        .admin-btn {
            position: absolute;
            top: 0;
            right: 0;
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 8px;
            cursor: pointer;
            opacity: 0;
            transition: all 0.15s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #667eea;
            font-size: 1.2rem;
        }

        .admin-btn:hover {
            opacity: 1;
            background: rgba(102, 126, 234, 0.1);
            border-color: rgba(102, 126, 234, 0.3);
        }

        .intro-box {
            background: rgba(102, 126, 234, 0.1);
            border: 1px solid rgba(102, 126, 234, 0.2);
            border-radius: 12px;
            padding: 1.5rem;
            margin: 0 auto;
            max-width: 700px;
        }

        .intro-box p {
            margin-bottom: 0.75rem;
            line-height: 1.6;
        }

        .intro-box p:last-child {
            margin-bottom: 0;
        }

        .required {
            color: #ef4444;
            font-weight: 700;
        }

        .form-section {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
            transition: all 0.15s ease;
        }

        .form-section:hover {
            border-color: rgba(102, 126, 234, 0.3);
            box-shadow: 0 8px 32px rgba(102, 126, 234, 0.1);
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #667eea;
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 2px solid rgba(102, 126, 234, 0.2);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group:last-child {
            margin-bottom: 0;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: #e4e4e7;
        }

        input[type="text"],
        input[type="url"],
        input[type="date"],
        select,
        textarea {
            width: 100%;
            padding: 0.75rem 1rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            color: #e4e4e7;
            font-size: 1rem;
            font-family: inherit;
            transition: all 0.15s ease;
        }

        input:focus,
        select:focus,
        textarea:focus {
            outline: none;
            border-color: #667eea;
            background: rgba(102, 126, 234, 0.05);
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        textarea {
            min-height: 120px;
            resize: vertical;
        }

        select {
            cursor: pointer;
        }

        .helper-text {
            margin-top: 0.5rem;
            font-size: 0.875rem;
            color: #a1a1aa;
            line-height: 1.4;
        }

        .char-count {
            color: #667eea;
            font-weight: 600;
        }

        .radio-group {
            display: flex;
            gap: 1rem;
            margin-top: 0.75rem;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem 1.25rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.15s ease;
            flex: 1;
        }

        .radio-option:hover {
            background: rgba(102, 126, 234, 0.1);
            border-color: rgba(102, 126, 234, 0.3);
        }

        .radio-option input[type="radio"] {
            width: auto;
            margin: 0;
            cursor: pointer;
        }

        .radio-option input[type="radio"]:checked + span {
            color: #667eea;
            font-weight: 600;
        }

        .submit-section {
            text-align: center;
            margin: 3rem 0;
        }

        button[type="submit"] {
            padding: 1rem 3rem;
            font-size: 1.1rem;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.15s ease;
            box-shadow: 0 4px 20px rgba(102, 126, 234, 0.3);
        }

        button[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(102, 126, 234, 0.4);
        }

        button[type="submit"]:active {
            transform: translateY(0);
        }

        button[type="submit"]:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        footer {
            text-align: center;
            padding: 2rem 0;
            color: #71717a;
            font-size: 0.875rem;
        }

        /* Loading state */
        .loading {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(26, 26, 46, 0.95);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 9999;
        }

        .loading.active {
            display: flex;
        }

        .spinner {
            width: 50px;
            height: 50px;
            border: 4px solid rgba(255, 255, 255, 0.1);
            border-top: 4px solid #667eea;
            border-radius: 50%;
            animation: spin 0.6s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem 0.5rem;
            }

            .logo {
                font-size: 2rem;
            }

            .form-section {
                padding: 1.5rem;
            }

            .radio-group {
                flex-direction: column;
            }

            .admin-btn {
                width: 35px;
                height: 35px;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <a href="/admin/login" class="admin-btn" title="Admin Login">🔐</a>
            <h1 class="logo">777heaven Records</h1>
            <p class="tagline">Pitching Portal</p>
            
            <div class="intro-box">
                <p>Submit your pitch <strong>at least 2-3 weeks before release</strong>. Fields marked with <span class="required">*</span> are required.</p>
                <p><strong>IMPORTANT:</strong> Include complete artist info and high-quality images.</p>
            </div>
        </header>

        <form action="/submit" method="POST" id="submission-form">
            <!-- Artist Information -->
            <div class="form-section">
                <div class="section-title">Artist Information</div>
                
                <div class="form-group">
                    <label for="primaryArtist">Primary Artist(s) <span class="required">*</span></label>
                    <input type="text" id="primaryArtist" name="primaryArtist" required autocomplete="name">
                </div>

                <div class="form-group">
                    <label for="featuringArtist">Featuring Artist(s)</label>
                    <input type="text" id="featuringArtist" name="featuringArtist" autocomplete="off">
                </div>

                <div class="form-group">
                    <label for="artistImages">Artist Image(s) Link <span class="required">*</span></label>
                    <input type="url" id="artistImages" name="artistImages" required placeholder="https://drive.google.com/...">
                    <div class="helper-text">Upload high-resolution photo with plain background, preferably square format. Paste Google Drive folder link.</div>
                </div>

                <div class="form-group">
                    <label for="artistBio">Artist Bio <span class="required">*</span></label>
                    <textarea id="artistBio" name="artistBio" required placeholder="Concise introduction highlighting background, musical style, and notable achievements..." maxlength="1000"></textarea>
                    <div class="helper-text"><span class="char-count" data-target="artistBio">0</span> / 1000 characters</div>
                </div>

                <div class="form-group">
                    <label for="socials">Social Media Links <span class="required">*</span></label>
                    <textarea id="socials" name="socials" required placeholder="Instagram: https://...&#10;Spotify: https://...&#10;TikTok: https://..."></textarea>
                    <div class="helper-text">One link per line</div>
                </div>
            </div>

            <!-- Release Information -->
            <div class="form-section">
                <div class="section-title">Release Details</div>

                <div class="form-group">
                    <label for="productTitle">Product Title <span class="required">*</span></label>
                    <input type="text" id="productTitle" name="productTitle" required>
                </div>

                <div class="form-group">
                    <label for="format">Format <span class="required">*</span></label>
                    <select id="format" name="format" required>
                        <option value="">Select format</option>
                        <option value="Single">Single</option>
                        <option value="EP">EP</option>
                        <option value="Album">Album</option>
                    </select>
                </div>

                <div class="form-group" id="focusTrackGroup" style="display: none;">
                    <label for="focusTrack">Focus Track</label>
                    <input type="text" id="focusTrack" name="focusTrack">
                    <div class="helper-text">Required for EP or Album</div>
                </div>

                <div class="form-group">
                    <label for="upc">UPC/EAN</label>
                    <input type="text" id="upc" name="upc" placeholder="123456789012">
                    <div class="helper-text">ONErpm Dashboard → Latest Releases → select track → View UPC and ISRC</div>
                </div>

                <div class="form-group">
                    <label for="spotifyUri">Spotify URI</label>
                    <input type="text" id="spotifyUri" name="spotifyUri" placeholder="spotify:track:...">
                    <div class="helper-text">Spotify for Artists → Music → select track → (…) → Copy Spotify URI</div>
                </div>

                <div class="form-group">
                    <label for="releaseDate">Release Date <span class="required">*</span></label>
                    <input type="date" id="releaseDate" name="releaseDate" required>
                    <div class="helper-text" id="release-date-warning" style="display: none; color: #ef4444; font-weight: 600;"></div>
                </div>

                <div class="form-group">
                    <label for="genre">Genre <span class="required">*</span></label>
                    <select id="genre" name="genre" required>
                        <option value="">Select genre</option>
                        <option value="Indie Rock">Indie Rock</option>
                        <option value="Dream Pop">Dream Pop</option>
                        <option value="Lo-Fi">Lo-Fi / Bedroom Pop</option>
                        <option value="Folk">Indie Folk / Singer-Songwriter</option>
                        <option value="Electronic">Indie Electronic / Synth Pop</option>
                        <option value="Alternative">Alternative / Art Rock</option>
                        <option value="Post-Punk">Post-Punk / Garage Rock</option>
                        <option value="Shoegaze">Shoegaze / Noise Pop</option>
                        <option value="Emo">Emo / Math Rock</option>
                        <option value="Other">Other</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="vibe">Vibe/Mood <span class="required">*</span></label>
                    <select id="vibe" name="vibe" required>
                        <option value="">Select vibe</option>
                        <option value="Chill">Chill / Laid-back</option>
                        <option value="Energetic">Energetic / Upbeat</option>
                        <option value="Melancholic">Melancholic / Introspective</option>
                        <option value="Dark">Dark / Moody</option>
                        <option value="Dreamy">Dreamy / Ethereal</option>
                        <option value="Raw">Raw / Emotional</option>
                        <option value="Nostalgic">Nostalgic / Vintage</option>
                        <option value="Experimental">Experimental / Avant-garde</option>
                    </select>
                </div>
            </div>

            <!-- Content Details -->
            <div class="form-section">
                <div class="section-title">Content & Marketing</div>

                <div class="form-group">
                    <label for="songDescription">Song Description <span class="required">*</span></label>
                    <textarea id="songDescription" name="songDescription" required placeholder="Brief summary including theme, mood, and unique elements that make it stand out..." maxlength="500"></textarea>
                    <div class="helper-text"><span class="char-count" data-target="songDescription">0</span> / 500 characters</div>
                </div>

                <div class="form-group">
                    <label for="marketingBlurb">Marketing Blurb <span class="required">*</span></label>
                    <textarea id="marketingBlurb" name="marketingBlurb" required placeholder="Short, engaging pitch focusing on unique sound, themes, or story for playlists and media..." maxlength="300"></textarea>
                    <div class="helper-text"><span class="char-count" data-target="marketingBlurb">0</span> / 300 characters</div>
                </div>

                <div class="form-group">
                    <label for="keywords">Keywords <span class="required">*</span></label>
                    <input type="text" id="keywords" name="keywords" required placeholder="heartbreak, yearning, moving-on, hugot">
                    <div class="helper-text">Comma-separated keywords</div>
                </div>

                <div class="form-group">
                    <label for="canvasLink">Spotify Canvas Link</label>
                    <input type="url" id="canvasLink" name="canvasLink" placeholder="https://drive.google.com/...">
                    <div class="helper-text">Google Drive link to your Spotify Canvas (optional)</div>
                </div>
            </div>

            <!-- Technical Information -->
            <div class="form-section">
                <div class="section-title">Technical Information</div>

                <div class="form-group">
                    <label>Is your audio mixed in Dolby Atmos? <span class="required">*</span></label>
                    <div class="radio-group">
                        <label class="radio-option">
                            <input type="radio" id="dolbyYes" name="dolbyAtmos" value="Yes" required>
                            <span>Yes</span>
                        </label>
                        <label class="radio-option">
                            <input type="radio" id="dolbyNo" name="dolbyAtmos" value="No" required>
                            <span>No</span>
                        </label>
                    </div>
                    <div class="helper-text">Apple Music requires this information</div>
                </div>

                <div class="form-group">
                    <label>Is this your debut release?</label>
                    <div class="radio-group">
                        <label class="radio-option">
                            <input type="radio" id="debutYes" name="debutRelease" value="Yes">
                            <span>Yes</span>
                        </label>
                        <label class="radio-option">
                            <input type="radio" id="debutNo" name="debutRelease" value="No">
                            <span>No</span>
                        </label>
                    </div>
                </div>
            </div>

            <div class="submit-section">
                <button type="submit" id="submit-btn">Submit Pitch</button>
            </div>
        </form>

        <footer>
            <p>777heaven Records © 2026</p>
        </footer>
    </div>

    <!-- Loading Overlay -->
    <div class="loading" id="loadingOverlay">
        <div class="spinner"></div>
    </div>

    <script>
        // Smooth page load
        document.addEventListener('DOMContentLoaded', function() {
            document.body.style.opacity = '1';
        });

        // Character counter with smooth updates
        const textareas = document.querySelectorAll('textarea[maxlength]');
        textareas.forEach(textarea => {
            const counter = document.querySelector(`.char-count[data-target="${textarea.id}"]`);
            if (counter) {
                textarea.addEventListener('input', () => {
                    counter.textContent = textarea.value.length;
                    // Visual feedback when approaching limit
                    if (textarea.value.length > textarea.maxLength * 0.9) {
                        counter.style.color = '#ef4444';
                    } else {
                        counter.style.color = '#667eea';
                    }
                });
            }
        });

        // Show focus track for EP/Album with smooth animation
        const formatSelect = document.getElementById('format');
        const focusTrackGroup = document.getElementById('focusTrackGroup');
        const focusTrackInput = document.getElementById('focusTrack');
        
        formatSelect.addEventListener('change', () => {
            if (formatSelect.value === 'EP' || formatSelect.value === 'Album') {
                focusTrackGroup.style.display = 'block';
                focusTrackInput.required = true;
                setTimeout(() => {
                    focusTrackGroup.style.opacity = '1';
                    focusTrackGroup.style.transform = 'translateY(0)';
                }, 10);
            } else {
                focusTrackGroup.style.opacity = '0';
                focusTrackGroup.style.transform = 'translateY(-10px)';
                setTimeout(() => {
                    focusTrackGroup.style.display = 'none';
                    focusTrackInput.required = false;
                }, 150);
            }
        });

        // Add smooth transition styles to focus track group
        focusTrackGroup.style.transition = 'all 0.15s ease';
        focusTrackGroup.style.opacity = '0';
        focusTrackGroup.style.transform = 'translateY(-10px)';

        // Release date validation with improved UX
        const releaseDateInput = document.getElementById('releaseDate');
        const releaseDateWarning = document.getElementById('release-date-warning');
        
        releaseDateInput.addEventListener('change', () => {
            const selectedDate = new Date(releaseDateInput.value);
            const today = new Date();
            const twoWeeksFromNow = new Date(today.getTime() + (14 * 24 * 60 * 60 * 1000));
            
            if (selectedDate < twoWeeksFromNow) {
                releaseDateWarning.textContent = '⚠️ Warning: Less than 2 weeks notice. Priority consideration may be limited.';
                releaseDateWarning.style.display = 'block';
                releaseDateWarning.style.animation = 'fadeIn 0.3s ease-out';
            } else {
                releaseDateWarning.style.display = 'none';
            }
        });

        // Enhanced form submission with loading overlay
        const form = document.getElementById('submission-form');
        const submitBtn = document.getElementById('submit-btn');
        const loadingOverlay = document.getElementById('loadingOverlay');
        
        form.addEventListener('submit', (e) => {
            submitBtn.textContent = '✨ Submitting...';
            submitBtn.disabled = true;
            loadingOverlay.classList.add('active');
        });

        // Set minimum date to today
        const today = new Date().toISOString().split('T')[0];
        releaseDateInput.setAttribute('min', today);

        // Smooth scroll to first error on validation fail
        form.addEventListener('invalid', (e) => {
            e.preventDefault();
            const firstInvalid = form.querySelector(':invalid');
            if (firstInvalid) {
                firstInvalid.scrollIntoView({ behavior: 'smooth', block: 'center' });
                firstInvalid.focus();
            }
        }, true);

        // Add floating label effect on focus
        const inputs = document.querySelectorAll('input, select, textarea');
        inputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.style.transform = 'scale(1.01)';
            });
            input.addEventListener('blur', function() {
                this.style.transform = 'scale(1)';
            });
        });
    </script>
</body>
</html>
